window.onload = () => {
    let makeEventButton = document.querySelector("#make-event");
    let eventForm = document.querySelector("#event-form");
    makeEventButton.onclick = () => {
        // TODO form toggling
    };
};